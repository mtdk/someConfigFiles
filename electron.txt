#install electron to ubuntu18.04
sudo install npm

sudo npm install -g electron

electron -v

sudo npm update electron -g

=============================
uninstall electron

sudo npm uninstall electron
